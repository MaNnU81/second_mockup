import { type ToolService } from './ToolService';
/**
 * The tool handling the transformations via the adorner.
 *
 * @type {*}
 */
export declare class PointerTool {
    toolService: ToolService;
    adorner: any;
    handle: any;
    constructor(toolService: ToolService);
    tryActivate(): boolean;
    start(point: any, meta: any, nativeEvent: any): void;
    move(point: any, meta: any, nativeEvent: any): void;
    end(point: any, meta: any, nativeEvent: any): void;
    getCursor(p: any): any;
}
