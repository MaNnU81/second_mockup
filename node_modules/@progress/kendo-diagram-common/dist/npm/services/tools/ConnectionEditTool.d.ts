import { type ToolService } from './ToolService';
import { type Connection } from './../../dom/Connection';
export declare class ConnectionEditTool {
    toolService: ToolService;
    type: string;
    _c: Connection;
    handle: any;
    handleName: any;
    constructor(toolService: ToolService);
    tryActivate(_p: any, meta: any): boolean;
    start(point: any, meta: any, nativeEvent: any): void;
    move(point: any, meta: any, nativeEvent: any): boolean;
    end(point: any, meta: any, nativeEvent: any): void;
    getCursor(): string;
}
