import { type Connection } from './../dom/Connection';
import { type Connector } from './../dom/Connector';
import { type Diagram } from './../Diagram';
export declare class DeleteConnectionUnit {
    connection: Connection;
    diagram: Diagram;
    targetConnector: Connector;
    title: string;
    constructor(connection: Connection);
    undo(): void;
    redo(): void;
}
