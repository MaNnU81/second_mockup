import { LinearConnectionRouter } from './LinearConnectionRouter';
import { type Connection } from './../../dom/Connection';
/**
 * A simple poly-linear routing which does not alter the intermediate points.
 * Does hold the underlying hit, bounds....logic.
 *
 * @type {*|Object|void|extend|Zepto.extend|b.extend}
 */
export declare class PolylineRouter extends LinearConnectionRouter {
    constructor(connection: Connection);
    route(): void;
}
