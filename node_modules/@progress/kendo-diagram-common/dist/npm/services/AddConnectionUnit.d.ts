import { type Connection } from './../dom/Connection';
import { type Diagram } from './../Diagram';
export declare class AddConnectionUnit {
    connection: Connection;
    diagram: Diagram;
    title: string;
    constructor(connection: Connection, diagram: Diagram);
    undo(): void;
    redo(): void;
}
