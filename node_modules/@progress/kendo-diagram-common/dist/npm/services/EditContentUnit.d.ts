import { type Connection } from '../dom/Connection';
import { type Diagram } from '../Diagram';
import { type Shape } from '../dom/Shape';
export declare class EditContentUnit {
    shape: Shape | Connection;
    diagram: Diagram;
    title: string;
    nextText: string;
    prevText: string;
    constructor(shape: Shape | Connection, text: string);
    undo(): void;
    redo(): void;
}
