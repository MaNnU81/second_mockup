"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTemplate = getTemplate;
const functions_1 = require("../../utils/functions");
const template_service_1 = require("./template-service");
function getTemplate(options = {}) {
    let template;
    if (options.visual) {
        options.visual = template = template_service_1.TemplateService.compile(options.visual, options);
    }
    else if (options.template) {
        options.template = template = template_service_1.TemplateService.compile(options.template, options);
    }
    else if ((0, functions_1.isFunction)(options.content)) {
        template = options.content;
    }
    return template;
}
