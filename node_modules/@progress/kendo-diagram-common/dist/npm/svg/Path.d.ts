import { drawing as d } from '@progress/kendo-drawing';
import { VisualBase } from './VisualBase';
import { AutoSizeableMixin } from './AutoSizeableMixin';
import { MarkerPathMixin } from './MarkerPathMixin';
/**
 * Represents an SVG path element with auto-sizing and marker capabilities.
 * Combines VisualBase with AutoSizeableMixin and MarkerPathMixin functionality.
 */
export declare class Path extends VisualBase {
    /** @hidden */
    _setScale: typeof AutoSizeableMixin._setScale;
    /** @hidden */
    _setTranslate: typeof AutoSizeableMixin._setTranslate;
    /** @hidden */
    _initSize: typeof AutoSizeableMixin._initSize;
    /** @hidden */
    _updateSize: typeof AutoSizeableMixin._updateSize;
    /** @hidden */
    _capMap: {
        start: string;
        end: string;
    };
    /** @hidden */
    _getPath: typeof MarkerPathMixin._getPath;
    /** @hidden */
    _normalizeMarkerOptions: typeof MarkerPathMixin._normalizeMarkerOptions;
    /** @hidden */
    _removeMarker: typeof MarkerPathMixin._removeMarker;
    /** @hidden */
    _createMarkers: typeof MarkerPathMixin._createMarkers;
    /** @hidden */
    _createMarker: typeof MarkerPathMixin._createMarker;
    /** @hidden */
    _positionMarker: typeof MarkerPathMixin._positionMarker;
    /** @hidden */
    _redrawMarker: typeof MarkerPathMixin._redrawMarker;
    /** @hidden */
    _redrawMarkers: typeof MarkerPathMixin._redrawMarkers;
    /**
     * The drawing group container that holds the path and its markers.
     */
    container: d.Group;
    /**
     * Creates a new Path instance.
     * @param options - Configuration options for the path
     */
    constructor(options: any);
    /**
     * Gets the drawing container that holds the path and its markers.
     * @returns The drawing group container
     */
    drawingContainer(): d.Group;
    /**
     * Gets or sets the path data string for the SVG path.
     * @param value The SVG path data string to set, or undefined to get current data
     * @returns The current path data string when called as getter
     */
    data(value: any): any;
    /**
     * Redraws the path with updated options.
     * @param options - Updated configuration options
     */
    redraw(options: any): void;
    /**
     * @hidden
     * Creates the drawing elements for the path.
     */
    _createElements(): void;
    /**
     * @hidden
     * Sets the path data for the drawing element.
     * @param data - The SVG path data string
     */
    _setData(data: any): void;
}
