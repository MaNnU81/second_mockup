import { drawing as d } from '@progress/kendo-drawing';
import { AutoSizeableMixin } from './AutoSizeableMixin';
import { VisualBase } from './VisualBase';
import { ShapeContent } from './../types';
/** @hidden */
export declare const defaultTextOptions: ShapeContent;
/**
 * A visual element that renders text content.
 * Extends VisualBase and provides functionality for creating and managing text graphics with font styling.
 */
export declare class TextBlock extends VisualBase {
    /** @hidden */
    _setScale: typeof AutoSizeableMixin._setScale;
    /** @hidden */
    _setTranslate: typeof AutoSizeableMixin._setTranslate;
    /** @hidden */
    _initSize: typeof AutoSizeableMixin._initSize;
    /** @hidden */
    _updateSize: typeof AutoSizeableMixin._updateSize;
    /** The underlying drawing element for the text. */
    drawingElement: d.Text | d.Group;
    /** Array of text elements for multi-line text support. */
    textElements: d.Text[];
    /** Whether the text block can be aligned within its container. */
    alignable: boolean;
    /** @hidden */
    readonly name: string;
    /**
     * Creates a new TextBlock instance.
     * @param options Configuration options for the text block
     */
    constructor(options: any);
    /** @hidden */
    _initText(): void;
    /** @hidden */
    _fill(fillOptions?: any): void;
    /** @hidden */
    _setFill(fill: {
        color: string;
        opacity?: number;
    }): void;
    /** @hidden */
    _stroke(strokeOptions?: any): void;
    /** @hidden */
    _font(): void;
    /**
     * Gets or sets the text content of the text block.
     * @param text The text content to set, or undefined to get current content
     * @returns The current text content when called as getter
     */
    content(text: any): void;
    /**
     * Redraws the text block with updated options.
     * @param options New options to apply to the text block
     */
    redraw(options: any): void;
}
