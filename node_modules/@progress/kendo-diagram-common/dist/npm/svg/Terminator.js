"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Terminator = void 0;
const utils_1 = require("./utils");
const Rectangle_1 = require("./Rectangle");
/**
 * A visual element that renders a terminator symbol with rounded ends.
 * Extends Rectangle and provides functionality for creating start/end flowchart shapes.
 */
class Terminator extends Rectangle_1.Rectangle {
    /** @hidden */
    _drawPath() {
        const { width, height } = (0, utils_1.sizeOptionsOrDefault)(this.options);
        const drawingElement = this.drawingElement;
        const r = height / 2;
        drawingElement
            .moveTo(r, 0)
            .lineTo(width - 2 * r, 0)
            .arcTo([width - 2 * r, height], r, r, false, false)
            .lineTo(r, height)
            .arcTo([r, 0], r, r, false, false)
            .close();
    }
}
exports.Terminator = Terminator;
