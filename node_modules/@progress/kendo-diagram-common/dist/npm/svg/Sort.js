"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Sort = void 0;
const utils_1 = require("./utils");
const MultiPathShape_1 = require("./MultiPathShape");
const defaultOptions = {
    shapesOffsetRatio: 0.015,
};
/**
 * Represents a Sort shape, a flowchart element used to indicate sorting operations.
 * Extends MultiPathShape with two triangular shapes arranged vertically.
 */
class Sort extends MultiPathShape_1.MultiPathShape {
    /**
     * Creates a new Sort instance.
     * @param options - Configuration options for the sort shape
     */
    constructor(options) {
        super({ ...defaultOptions, ...options });
    }
    /**
     * @hidden
     * Draws the sort shape path as two triangular shapes.
     */
    _drawPath() {
        const drawingElement = this.drawingElement;
        const { width, height } = (0, utils_1.sizeOptionsOrDefault)(this.options);
        const strokeWidth = this.options.stroke.width || 0;
        const cornerRadius = this.options.cornerRadius;
        const shapesOffset = this.options.shapesOffsetRatio * height;
        const halfHeight = height / 2;
        const firstShape = [
            { x: width / 2, y: 0 },
            { x: width, y: halfHeight - shapesOffset },
            { x: 0, y: halfHeight - shapesOffset }
        ];
        (0, utils_1.drawRoundedPolygon)(drawingElement, firstShape, [cornerRadius / 3, cornerRadius, cornerRadius]);
        const secondShape = [
            { x: width / 2, y: height },
            { x: 0, y: halfHeight + shapesOffset },
            { x: width, y: halfHeight + shapesOffset }
        ];
        (0, utils_1.drawRoundedPolygon)(drawingElement, secondShape, [cornerRadius / 3, cornerRadius, cornerRadius]);
        (0, utils_1.fitToSize)(drawingElement, width, height, strokeWidth);
    }
}
exports.Sort = Sort;
