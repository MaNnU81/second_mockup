"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Extract = void 0;
const utils_1 = require("./utils");
const Rectangle_1 = require("./Rectangle");
/**
 * Represents an Extract shape, a flowchart element used to indicate extraction of data or a process step.
 * Extends the Rectangle class with a custom triangular path.
 */
class Extract extends Rectangle_1.Rectangle {
    /**
     * @hidden
     * Draws the extract shape path as a triangle.
     */
    _drawPath() {
        const drawingElement = this.drawingElement;
        const { width, height } = (0, utils_1.sizeOptionsOrDefault)(this.options);
        const strokeWidth = this.options.stroke.width || 0;
        const r = this.options.cornerRadius || 0;
        const points = [
            { x: width / 2, y: 0 },
            { x: width, y: height },
            { x: 0, y: height }
        ];
        const inflated = (0, utils_1.inflatePolygon)(points, r);
        (0, utils_1.drawRoundedPolygon)(drawingElement, inflated, r);
        (0, utils_1.fitToSize)(drawingElement, width, height, strokeWidth);
    }
}
exports.Extract = Extract;
