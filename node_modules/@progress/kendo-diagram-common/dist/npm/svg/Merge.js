"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Merge = void 0;
const utils_1 = require("./utils");
const Rectangle_1 = require("./Rectangle");
/**
 * Represents a Merge shape, a flowchart element used to indicate the merging
 * of multiple processes or data streams. Extends Rectangle with a triangular path.
 */
class Merge extends Rectangle_1.Rectangle {
    /**
     * @hidden
     * Draws the merge shape path as an inverted triangle.
     */
    _drawPath() {
        const drawingElement = this.drawingElement;
        const { width, height } = (0, utils_1.sizeOptionsOrDefault)(this.options);
        const strokeWidth = this.options.stroke.width || 0;
        const points = [
            { x: 0, y: 0 },
            { x: width, y: 0 },
            { x: width / 2, y: height }
        ];
        const inflated = (0, utils_1.inflatePolygon)(points, this.options.cornerRadius);
        (0, utils_1.drawRoundedPolygon)(drawingElement, inflated, this.options.cornerRadius);
        (0, utils_1.fitToSize)(drawingElement, width, height, strokeWidth);
    }
}
exports.Merge = Merge;
