"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TextBlock = exports.defaultTextOptions = void 0;
const kendo_drawing_1 = require("@progress/kendo-drawing");
const AutoSizeableMixin_1 = require("./AutoSizeableMixin");
const VisualBase_1 = require("./VisualBase");
const deep_extend_1 = require("./../utils/deep-extend");
const utils_1 = require("./../utils");
const utils_2 = require("./utils");
const constants_1 = require("../constants");
const textColor = (options) => {
    if (options && options.color) {
        options = (0, deep_extend_1.deepExtend)({}, options, {
            fill: {
                color: options.color
            }
        });
    }
    return options;
};
/** @hidden */
exports.defaultTextOptions = {
    fontSize: 15,
    fontFamily: 'sans-serif',
    lineSpacing: 0,
    textWrap: 'nowrap',
    padding: (0, utils_2.getSpacing)(0),
    relativePadding: (0, utils_2.getSpacing)(0),
    stroke: {
        width: 0
    },
    fill: {
        color: 'black'
    },
    autoSize: true
};
/**
 * A visual element that renders text content.
 * Extends VisualBase and provides functionality for creating and managing text graphics with font styling.
 */
class TextBlock extends VisualBase_1.VisualBase {
    /**
     * Creates a new TextBlock instance.
     * @param options Configuration options for the text block
     */
    constructor(options) {
        options = (0, deep_extend_1.deepExtend)({}, exports.defaultTextOptions, options);
        options = textColor(options);
        super(options);
        /** Array of text elements for multi-line text support. */
        this.textElements = [];
        /** Whether the text block can be aligned within its container. */
        this.alignable = true;
        /** @hidden */
        this.name = 'TextBlock';
        this._setScale = AutoSizeableMixin_1.AutoSizeableMixin._setScale.bind(this);
        this._setTranslate = AutoSizeableMixin_1.AutoSizeableMixin._setTranslate.bind(this);
        this._initSize = AutoSizeableMixin_1.AutoSizeableMixin._initSize.bind(this);
        this._updateSize = AutoSizeableMixin_1.AutoSizeableMixin._updateSize.bind(this);
        this._font();
        this._initText();
        this._initSize();
    }
    /** @hidden */
    _initText() {
        const options = this.options;
        this.textElements.length = 0;
        this.drawingElement = new kendo_drawing_1.drawing.Text((0, utils_1.defined)(options.text) ? options.text : constants_1.EMPTY, new kendo_drawing_1.geometry.Point(), {
            font: options.font
        });
        this.textElements.push(this.drawingElement);
        this._fill();
        this._stroke();
    }
    /** @hidden */
    _fill(fillOptions) {
        const fill = this._evalFillOptions(fillOptions);
        if (fill.gradient) {
            const gradient = fill.gradient;
            const GradientClass = (gradient.type === 'radial' ? kendo_drawing_1.drawing.RadialGradient : kendo_drawing_1.drawing.LinearGradient);
            this.textElements.forEach((el) => {
                el.fill(new GradientClass(gradient));
            });
        }
        else {
            this._setFill(fill);
        }
    }
    /** @hidden */
    _setFill(fill) {
        this.textElements.forEach((el) => {
            el.fill(fill.color, fill.opacity);
        });
    }
    /** @hidden */
    _stroke(strokeOptions) {
        const stroke = this._evalStrokeOptions(strokeOptions);
        this.textElements.forEach(el => el.options.set('stroke', stroke));
    }
    /** @hidden */
    _font() {
        const options = this.options;
        if (options.fontFamily && (0, utils_1.defined)(options.fontSize)) {
            const fontOptions = [];
            if (options.fontStyle) {
                fontOptions.push(options.fontStyle);
            }
            if (options.fontWeight) {
                fontOptions.push(options.fontWeight);
            }
            fontOptions.push(options.fontSize + ((0, utils_1.isNumber)(options.fontSize) ? constants_1.PX : constants_1.EMPTY));
            fontOptions.push(options.fontFamily);
            options.font = fontOptions.join(constants_1.SPACE);
        }
        else {
            delete options.font;
        }
    }
    /**
     * Gets or sets the text content of the text block.
     * @param text The text content to set, or undefined to get current content
     * @returns The current text content when called as getter
     */
    content(text) {
        return this.drawingElement.content(text);
    }
    /**
     * Redraws the text block with updated options.
     * @param options New options to apply to the text block
     */
    redraw(options) {
        if (options) {
            let sizeChanged = false;
            const textOptions = this.options;
            options = textColor(options);
            super.redraw(options);
            if (options.fontFamily || (0, utils_1.defined)(options.fontSize) || options.fontStyle || options.fontWeight) {
                (0, deep_extend_1.deepExtend)(textOptions, {
                    fontFamily: options.fontFamily,
                    fontSize: options.fontSize,
                    fontStyle: options.fontStyle,
                    fontWeight: options.fontWeight
                });
                this._font();
                this.drawingElement.options.set('font', textOptions.font);
                sizeChanged = true;
            }
            if (options.text) {
                this.content(options.text);
                sizeChanged = true;
            }
            if (!this._updateSize(options) && sizeChanged) {
                this._initSize();
            }
        }
    }
}
exports.TextBlock = TextBlock;
