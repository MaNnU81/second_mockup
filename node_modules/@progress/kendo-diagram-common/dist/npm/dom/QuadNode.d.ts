import { Rect } from './../math';
import { QuadRoot } from './QuadRoot';
export declare class QuadNode extends QuadRoot {
    rect: Rect;
    children: QuadNode[];
    constructor(rect: any);
    inBounds(rect: any): boolean;
    overlapsBounds(rect: any): boolean;
    insert(shape: any, bounds: any): boolean;
    _initChildren(): void;
    hitTestRect(rect: any, exclude: any): boolean;
}
