"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DiagramElement = exports.getTextElementType = void 0;
const kendo_drawing_1 = require("@progress/kendo-drawing");
const get_template_1 = require("../services/template/get-template");
const constants_1 = require("./../constants");
const math_1 = require("./../math");
const svg_1 = require("./../svg");
const utils_1 = require("./../utils");
const Observable_1 = require("./../utils/Observable");
const deep_extend_1 = require("./../utils/deep-extend");
const utils_2 = require("./../utils");
const utils_3 = require("../utils/scroller/utils");
/** @hidden */
const getTextElementType = (options) => {
    let textWrap = options.textWrap;
    if (textWrap === undefined && (options.text.includes(constants_1.NEW_LINE_CHAR) || !linesFit(options, (0, svg_1.textRect)(options).size.width))) {
        textWrap = constants_1.WRAP;
    }
    if (options.border || options.position === constants_1.INLINE) {
        textWrap = constants_1.WRAP;
    }
    return textWrap !== constants_1.WRAP ? svg_1.TextBlock : svg_1.MultiLineTextBlock;
};
exports.getTextElementType = getTextElementType;
const linesFit = (options, maxWidth) => {
    const text = options.text || '';
    const styles = (0, svg_1.getFont)(options);
    return !text.split(constants_1.NEW_LINE_CHAR).find(line => kendo_drawing_1.drawing.util.measureText(line, styles).width > maxWidth);
};
const defaultOptions = {
    hover: {},
    cursor: constants_1.Cursors.grip,
    content: {
        align: 'center middle'
    },
    selectable: true,
    serializable: true,
    enable: true
};
/** @hidden */
class DiagramElement extends Observable_1.Observable {
    /**
     * Creates a new DiagramElement instance.
     * @param options Configuration options for the diagram element
     */
    constructor(options) {
        super();
        this.dataItem = (options || {}).dataItem;
        this.options = (0, deep_extend_1.deepExtend)({ id: (0, math_1.randomId)() }, defaultOptions, options);
        this.isSelected = false;
        this.visual = new svg_1.Group({
            id: this.options.id,
            autoSize: this.options.autoSize
        });
        this.id = this.options.id;
        this._template();
    }
    /** @hidden */
    _getCursor(point) {
        if (this.adorner) {
            return this.adorner._getCursor(point);
        }
        return this.options.cursor;
    }
    /**
     * Gets or sets the visibility of the diagram element.
     * @param value If provided, sets the visibility. If undefined, returns the current visibility.
     * @returns The current visibility when used as a getter
     */
    visible(value) {
        if ((0, utils_1.isUndefined)(value)) {
            return this.visual.visible();
        }
        else {
            this.visual.visible(value);
        }
    }
    /**
     * Gets or sets the bounds of the diagram element.
     * Base implementation returns null - should be overridden by subclasses.
     * @param value The bounds to set. If not provided, returns the current bounds.
     * @returns The bounds of the element or null
     */
    bounds(value) {
        if ((0, utils_1.isUndefined)(value)) {
            return null;
        }
        return null;
    }
    /**
     * Refreshes the visual representation of the diagram element.
     * Triggers a redraw of the visual component.
     */
    refresh() {
        this.visual.redraw();
    }
    /**
     * Sets the position of the diagram element.
     * @param point The new position point with x and y coordinates
     */
    position(point) {
        this.options.x = point.x;
        this.options.y = point.y;
        this.visual.position(point);
    }
    /**
     * Returns a string representation of the diagram element.
     * @returns The ID of the element as a string
     */
    toString() {
        return this.options.id;
    }
    /**
     * Serializes the diagram element to a JSON object.
     * @returns An object containing the element's options and data item information
     */
    serialize() {
        // the options json object describes the shape perfectly. So this object can serve as shape serialization.
        const json = (0, deep_extend_1.deepExtend)({}, { options: this.options });
        if (this.dataItem) {
            json.dataItem = this.dataItem.toString();
        }
        return json;
    }
    /** @hidden */
    _content(content) {
        if (content !== undefined) {
            const options = this.options;
            if ((0, utils_1.isString)(content)) {
                options.content.text = content;
            }
            else {
                (0, deep_extend_1.deepExtend)(options.content, content);
            }
            const contentOptions = options.content;
            let contentVisual = this._contentVisual;
            if (contentVisual && (0, exports.getTextElementType)(contentOptions).name !== contentVisual.name) {
                const drawingElement = contentVisual.drawingContainer();
                drawingElement.parent.remove(drawingElement);
                this._contentVisual = contentVisual = null;
            }
            if (!contentVisual) {
                this._createContentVisual(contentOptions);
            }
            else {
                this._updateContentVisual(contentOptions);
            }
        }
        return this.options.content.text;
    }
    /** @hidden */
    _createContentVisual(options) {
        if (options.text) {
            options.shapeSize = this.bounds();
            const Type = (0, exports.getTextElementType)(options);
            this._contentVisual = new Type(options);
            this._contentVisual._includeInBBox = false;
            this.visual.append(this._contentVisual);
        }
    }
    /** @hidden */
    _updateContentVisual(options) {
        this._contentVisual.redraw(options);
    }
    /** @hidden */
    _hitTest(point) {
        const bounds = this.bounds();
        return this.visible() && bounds.contains(point) && this.options.enable;
    }
    /** @hidden */
    _template() {
        if (this.options.content.template) {
            const data = this.dataItem || {};
            const templateOptions = (0, utils_3.extend)({}, this.options.content, {
                dataItem: this.dataItem || this.options.dataItem,
            });
            const elementTemplate = (0, get_template_1.getTemplate)(templateOptions);
            if ((0, utils_2.isFunction)(elementTemplate)) {
                this.options.content.text = elementTemplate(data);
            }
            else if ((0, utils_1.isString)(elementTemplate)) {
                this.options.content.text = elementTemplate;
            }
        }
    }
    /** @hidden */
    _canSelect() {
        return this.options.selectable !== false;
    }
    /**
     * Converts the diagram element to a JSON representation.
     * @returns Object containing the element's ID
     */
    toJSON() {
        return {
            id: this.options.id
        };
    }
}
exports.DiagramElement = DiagramElement;
