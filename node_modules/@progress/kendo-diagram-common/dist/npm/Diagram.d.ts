import { drawing as draw } from '@progress/kendo-drawing';
import { Observable } from './utils/Observable';
import { Shape, ShapesQuadTree, Connection, InactiveItemsCollection } from './dom';
import { Group } from './svg';
import { Point, Rect } from './math';
import { ResizingAdorner, ConnectorsAdorner, Selector, ToolService } from './services';
export declare class Diagram extends Observable {
    /** The HTML element that contains the diagram. */
    element: HTMLElement;
    /** The wrapper element for the diagram content. */
    wrapper: HTMLElement;
    /** The main layer group that contains all diagram elements. */
    mainLayer: Group;
    /** The canvas element used for rendering the diagram. */
    canvas: any;
    /** @hidden */
    _shapesQuadTree: ShapesQuadTree;
    /** @hidden */
    _pan: Point;
    /** @hidden */
    _adorners: any[];
    /** The layer group that contains adorners for selection and editing. */
    adornerLayer: Group;
    /** @hidden */
    _resizingAdorner: ResizingAdorner;
    /** @hidden */
    _connectorsAdorner: ConnectorsAdorner;
    /** The selector tool for area selection. */
    selector: Selector;
    /** @hidden */
    readonly _clipboard: any[];
    /** Indicates whether mouse handlers should be paused. */
    pauseMouseHandlers: boolean;
    /** @hidden */
    _userEvents: any;
    /** @hidden */
    /** The tool service that manages diagram tools and interactions. */
    toolService: ToolService;
    /** The scrollable container element. */
    scrollable: any;
    /** @hidden */
    _gesture: any;
    /** @hidden */
    _initialCenter: Point;
    /** @hidden */
    _zoom: number;
    /** @hidden */
    _isEditable: boolean;
    /** @hidden */
    readonly _connectionsDataMap: any;
    /** @hidden */
    readonly _dataMap: any;
    /** @hidden */
    readonly _inactiveShapeItems: InactiveItemsCollection;
    /** @hidden */
    readonly _selectedItems: any[];
    /** @hidden */
    _internalSelection: boolean;
    /** The toolbar element for diagram tools. */
    toolBar: any;
    /** Array of all shapes in the diagram. */
    readonly shapes: Shape[];
    /** Array of all connections in the diagram. */
    readonly connections: any[];
    /** Service for managing undo/redo operations. */
    undoRedoService: any;
    /** The scroller component for panning. */
    scroller: any;
    /** @hidden */
    _resizeHandler: any;
    /** @hidden */
    _shapes: any;
    /** @hidden */
    _connections: any;
    /** @hidden */
    _wheelHandler: any;
    /** @hidden */
    _keydownHandler: any;
    /** @hidden */
    _resizeObserver: any;
    /** The current size of the diagram viewport. */
    size: {
        width: number;
        height: number;
    };
    /** @hidden */
    _pauseMouseHandlers: boolean;
    /** @hidden */
    _copyOffset: number;
    /** @hidden */
    readonly _deferredConnectionUpdates: Promise<void>[];
    /** @hidden */
    _matrixInvert: any;
    /** @hidden */
    _matrix: any;
    /** @hidden */
    _layerMatrix: any;
    /** @hidden */
    _layerMatrixInvert: any;
    /** @hidden */
    _layouting: boolean;
    /** @hidden */
    _bindingRoots: boolean;
    /** The unique identifier of the diagram. */
    id: string;
    /** @hidden */
    _loadingShapes: boolean;
    /** @hidden */
    _loadingConnections: boolean;
    /** @hidden */
    _cachedTouchTarget: any;
    /** @hidden */
    _shapesRefreshHandler: any;
    /** @hidden */
    _shapesRequestStartHandler: any;
    /** @hidden */
    _shapesErrorHandler: any;
    /** @hidden */
    _connectionsRefreshHandler: any;
    /** @hidden */
    _connectionsRequestStartHandler: any;
    /** @hidden */
    _connectionsErrorHandler: any;
    /** @hidden */
    _suspended: number;
    /** @hidden */
    _refreshHandler: any;
    /** @hidden */
    _errorHandler: any;
    /** @hidden */
    _mouseoverHandler: any;
    /** @hidden */
    _mouseoutHandler: any;
    /** @hidden */
    _mouseMoveHandler: any;
    /** @hidden */
    _mouseDownHandler: any;
    /** @hidden */
    _mouseUpHandler: any;
    /** @hidden */
    _tooltipTimeOut: any;
    /**
     * @hidden
     *
     * The ruler component for measurements and guides.
     */
    ruler: any;
    /**
     * Creates a new Diagram instance.
     * @param element The HTML element that will contain the diagram
     * @param userOptions Configuration options for the diagram
     * @param themeOptions Theme-specific options
     */
    constructor(element: any, userOptions: any, themeOptions: any);
    /** @hidden */
    _createShape(dataItem: any, options: any): Shape;
    /** @hidden */
    _createConnection(dataItem: any, source?: any, target?: any): Connection;
    /** @hidden */
    _initElements(): void;
    /** @hidden */
    _initDefaults(userOptions: any): void;
    /** @hidden */
    _interactionDefaults(): void;
    /** @hidden */
    _initCanvas(): void;
    /** @hidden */
    _createHandlers(): void;
    _onMouseEnter(event: any): void;
    _onMouseLeave(event: any): void;
    /** @hidden */
    _initResizeObserver(): void;
    /** @hidden */
    _destroyResizeObserver(): void;
    /** @hidden */
    _dragStart(e: any): void;
    /** @hidden */
    _drag(e: any): void;
    /** @hidden */
    _dragEnd(e: any): void;
    /** @hidden */
    _mouseMove(e: any): void;
    /** @hidden */
    _domEvent: (nativeEvent: any) => void;
    /** @hidden */
    _mouseDown(): void;
    /** @hidden */
    _mouseUp(): void;
    /** @hidden */
    _tap(e: any): void;
    /** @hidden */
    _keydown(e: any): void;
    /** @hidden */
    _wheel(e: any): void;
    /** @hidden */
    _meta(e: any): {
        ctrlKey: any;
        metaKey: any;
        altKey: any;
        shiftKey: any;
        type: any;
    };
    /** @hidden */
    _eventPositions(e: any, start?: any): any;
    /** @hidden */
    _gestureStart(e: any): void;
    /** @hidden */
    _gestureChange(e: any): void;
    /** @hidden */
    _doubleTap(e: any): void;
    /** @hidden */
    _gestureEnd(): void;
    /** @hidden */
    _resize(): void;
    /** @hidden */
    _mouseover(e: any): void;
    /** @hidden */
    _mouseout(e: any): void;
    /** @hidden */
    _initTheme(themeOptions: any): void;
    /** @hidden */
    _createOptionElements(): void;
    /** @hidden */
    _createShapes(): void;
    /** @hidden */
    _createConnections(): void;
    /** @hidden */
    _findConnectionTarget(options: any): any;
    /**
     * Destroys the diagram and cleans up all resources.
     * Removes event listeners, destroys components, and clears all elements.
     */
    destroy(): void;
    /**
     * Destroys the scroller component and removes its element.
     */
    destroyScroller(): void;
    /**
     * Serializes the diagram to a JSON object containing shapes and connections.
     * @returns Object with shapes and connections arrays
     */
    save(): {
        shapes: any[];
        connections: any[];
    };
    /**
     * Sets focus to the diagram element.
     * @returns True if focus was set, undefined otherwise
     */
    focus(): boolean;
    /**
     * Loads diagram data and recreates shapes and connections.
     * @param options The diagram options containing shapes and connections data
     */
    load(options: any): void;
    /**
     * Sets options for the diagram by deep extending the current options.
     * @param options The options to merge with current diagram options
     */
    setOptions(options: any): void;
    /**
     * Clears the diagram by removing all selections and elements, then reinitializes.
     */
    clear(): void;
    /**
     * Determines whether the the two items are connected.
     *
     * @param source Shape, Connector, Point.
     * @param target Shape, Connector, Point.
     * @returns true if the two items are connected.
     */
    connected(source: any, target: any): boolean;
    /**
     * Adds connection to the diagram.
     *
     * @param connection Connection.
     * @param undoable Boolean.
     * @returns The newly created connection.
     */
    addConnection(connection: Connection, undoable?: boolean): Connection;
    /**
     * Adds shape to the diagram.
     *
     * @param item Shape, Point. If point is passed it will be created new Shape and positioned at that point.
     * @param options. The options to be passed to the newly created Shape.
     * @returns The newly created shape.
     */
    addShape(item: any, undoable?: any): any;
    /**
     * Removes items (or single item) from the diagram.
     *
     * @param items DiagramElement, Array of Items.
     * @param undoable.
     */
    remove(items: any, undoable?: any): void;
    /** @hidden */
    _addConnection(connection: any, undoable?: any): any;
    /** @hidden */
    _addShape(shape: any, undoable?: any): any;
    /** @hidden */
    _parseBounds(bounds: any): void;
    /** @hidden */
    _shouldRefresh(): boolean;
    /** @hidden */
    _suspendModelRefresh(): void;
    /** @hidden */
    _resumeModelRefresh(): void;
    /** @hidden */
    _triggerRemove(items: any): any[];
    /** @hidden */
    _addConnections(connections: any, undoable: any): void;
    /** @hidden */
    _addConnectionDataItem(dataItem: any, undoable: any): void;
    /** @hidden */
    _validateConnector(value: any): any;
    /** @hidden */
    _addDataItems(items: any, parent: any): void;
    /**
     * Creates a connection between two endpoints (shapes, connectors, or points).
     * @param source The source endpoint
     * @param target The target endpoint
     * @param options Optional connection configuration
     * @returns The newly created connection
     */
    connect(source: any, target: any, options?: any): Connection;
    /**
     * Executes the next undoable action on top of the undo stack if any.
     */
    undo(): void;
    /**
     * Executes the previous undoable action on top of the redo stack if any.
     */
    redo(): void;
    /**
     * Selects items on the basis of the given input or returns the current selection if none.
     *
     * @param itemsOrRect DiagramElement, Array of elements, "All", false or Rect. A value 'false' will deselect everything.
     * @param options
     * @returns {Array} The currently selected items.
     */
    select(item?: any, options?: any): any[];
    /**
     * Selects all shapes and connections in the diagram.
     */
    selectAll(): void;
    /**
     * Selects shapes and connections within a rectangular area.
     * @param rect The rectangular area to select items within
     */
    selectArea(rect: any): void;
    /**
     * Deselects items from the current selection.
     * @param item The item(s) to deselect. If not provided, deselects all items.
     */
    deselect(item?: any): void;
    /**
     * Brings to front the passed items.
     *
     * @param items DiagramElement, Array of Items.
     * @param undoable. By default the action is undoable.
     */
    toFront(items: any, undoable?: any): void;
    /**
     * Sends to back the passed items.
     *
     * @param items DiagramElement, Array of Items.
     * @param undoable. By default the action is undoable.
     */
    toBack(items: any, undoable: any): void;
    /**
     * Bring into view the passed item(s) or rectangle.
     *
     * @param items DiagramElement, Array of Items, Rect.
     * @param options. align - controls the position of the calculated rectangle relative to the viewport.
     * "Center middle" will position the items in the center. animate - controls if the pan should be animated.
     */
    bringIntoView(item: any, options?: any): void;
    /**
     * Aligns shapes in the specified direction.
     * @param direction The alignment direction ('left', 'right', 'top', 'bottom')
     */
    alignShapes(direction: any): void;
    /**
     * Gets or sets the zoom level of the diagram.
     * @param zoom The zoom level to set (1.0 = 100%). If not provided, returns the current zoom.
     * @param options Options including the zoom point and metadata
     * @returns The current zoom level when used as a getter
     */
    zoom(zoom?: any, options?: any): number;
    /** @hidden */
    _getPan(pan: any): any;
    /**
     * Gets or sets the pan offset of the diagram.
     * @param pan The pan offset as a Point. If not provided, returns the current pan.
     * @param animate Whether to animate the pan operation
     * @returns The current pan offset when used as a getter
     */
    pan(pan?: any, animate?: any): Point;
    /**
     * Gets the viewport rectangle of the diagram.
     * @returns A Rect representing the current viewport
     */
    viewport(): Rect;
    /**
     * Copies the currently selected items to the clipboard.
     */
    copy(): void;
    /**
     * Cuts the currently selected items to the clipboard and removes them from the diagram.
     */
    cut(): void;
    /**
     * Pastes items from the clipboard into the diagram.
     */
    paste(): void;
    /** @hidden */
    _syncChanges(): void;
    /** @hidden */
    _syncConnectionChanges(): void;
    /** @hidden */
    _syncShapeChanges(): void;
    /** @hidden */
    _updateCopiedConnection(connection: any, sourceConnection: any, connectorName: any, mapping: any, offset: any): void;
    /**
     * Gets the bounding rectangle of the given items.
     *
     * @param items DiagramElement, Array of elements.
     * @param origin Boolean. Pass 'true' if you need to get the bounding box of the shapes without their rotation offset.
     * @returns {Rect} The bounding rectangle of the items.
     */
    boundingBox(items: any, origin?: any): Rect;
    /** @hidden */
    _containerOffset(): {
        top: number;
        left: number;
    };
    /**
     * Converts a point from document coordinates to view coordinates.
     * @param point The point in document coordinates
     * @returns The point in view coordinates
     */
    documentToView(point: any): Point;
    /**
     * Converts a point from view coordinates to document coordinates.
     * @param point The point in view coordinates
     * @returns The point in document coordinates
     */
    viewToDocument(point: any): Point;
    /**
     * Converts a point from view coordinates to model coordinates.
     * @param point The point in view coordinates
     * @returns The point in model coordinates
     */
    viewToModel(point: any): any;
    /**
     * Converts a point from model coordinates to view coordinates.
     * @param point The point in model coordinates
     * @returns The point in view coordinates
     */
    modelToView(point: any): any;
    /**
     * Converts a point from model coordinates to layer coordinates.
     * @param point The point in model coordinates
     * @returns The point in layer coordinates
     */
    modelToLayer(point: any): any;
    /**
     * Converts a point from layer coordinates to model coordinates.
     * @param point The point in layer coordinates
     * @returns The point in model coordinates
     */
    layerToModel(point: any): any;
    /**
     * Converts a point from document coordinates to model coordinates.
     * @param point The point in document coordinates
     * @returns The point in model coordinates
     */
    documentToModel(point: any): any;
    /**
     * Converts a point from model coordinates to document coordinates.
     * @param point The point in model coordinates
     * @returns The point in document coordinates
     */
    modelToDocument(point: any): Point;
    /** @hidden */
    _transformWithMatrix(point: any, matrix: any): any;
    /**
     * Performs a diagram layout of the given type.
     *
     * @param layoutType The layout algorithm to be applied (TreeLayout, LayeredLayout, SpringLayout).
     * @param options Layout-specific options.
     */
    layout(options?: any): void;
    /**
     * Gets a shape by its unique identifier.
     * @param {string} id The unique identifier of the shape
     * @returns {Shape} The shape with the specified ID, or undefined if not found
     */
    getShapeById(id: any): any;
    /**
     * Gets a shape by its model ID.
     * @param id The model ID of the shape
     * @returns The shape with the specified model ID, or undefined if not found
     */
    getShapeByModelId(id: any): any;
    /**
     * Gets a shape by its model UID.
     * @param uid The model UID of the shape
     * @returns The shape with the specified model UID, or undefined if not found
     */
    getShapeByModelUid(uid: any): any;
    /** @hidden */
    _extendLayoutOptions(options: any): void;
    /** @hidden */
    _selectionChanged(selected: any, deselected: any): void;
    /** @hidden */
    _getValidZoom(zoom: any): number;
    /** @hidden */
    _panTransform(pos?: any): void;
    /** @hidden */
    _finishPan(): void;
    /** @hidden */
    _storePan(pan: any): void;
    /** @hidden */
    _zoomMainLayer(): void;
    /** @hidden */
    _transformMainLayer(): void;
    /** @hidden */
    _storeLayerMatrix(canvasTransform: any): void;
    /** @hidden */
    _storeViewMatrix(): void;
    /** @hidden */
    _toIndex(items: any, indices: any): void;
    /** @hidden */
    _fixOrdering(result: any, toFront: any): void;
    /** @hidden */
    _getDiagramItems(items: any): any;
    /** @hidden */
    _addDataItemByUid(dataItem: any): any;
    /** @hidden */
    _addItem(item: any): void;
    /** @hidden */
    _toolBarClick(e: any): void;
    /** @hidden */
    _normalizePointZoom(point: any): any;
    /** @hidden */
    _initialize(): void;
    /** @hidden */
    _redrawConnections(): void;
    /** @hidden */
    _adorn(adorner: any, isActive: any): void;
    /** @hidden */
    _showConnectors(shape: any, value: any): void;
    /** @hidden */
    _updateAdorners(): void;
    /** @hidden */
    _refresh(): void;
    /** @hidden */
    _removeItem(item: any, undoable: any, removedConnections?: any): void;
    /** @hidden */
    _removeConnectionDataItem(item: any): void;
    /** @hidden */
    _removeShapeDataItem(item: any): void;
    /** @hidden */
    _removeShape(shape: any, undoable: any, removedConnections: any): void;
    /** @hidden */
    _removeConnection(connection: any, undoable: any): void;
    /** @hidden */
    _removeShapeConnections(shape: any): void;
    /** @hidden */
    _destroyToolBar(): void;
    /** @hidden */
    _destroyGlobalToolBar(): void;
    /** @hidden */
    _mobileOS(): any;
    /**
     * Exports the diagram's DOM visual representation for rendering or export purposes.
     * Creates a clipped group containing the canvas content with proper transformations.
     * @returns A drawing Group element containing the exported DOM visual
     */
    exportDOMVisual(): draw.Group;
    /**
     * Exports the diagram's visual representation with proper scaling based on zoom level.
     * Creates a scaled group containing the main layer content.
     * @returns A drawing Group element containing the exported visual with inverse zoom scaling
     */
    exportVisual(): draw.Group;
    /**
     * Updates the connection's underlying data model.
     * Calls the configured updateConnectionModel function if available.
     * @param connection The connection whose model should be updated
     * @param syncChanges Whether to synchronize changes immediately
     * @returns The result of the update operation, if any
     */
    updateConnectionModel(connection: Connection, syncChanges?: boolean): any;
    /**
     * Updates the shape's underlying data model.
     * Calls the configured updateShapeModel function if available.
     * @param shape The shape whose model should be updated
     * @param syncChanges Whether to synchronize changes immediately
     * @returns The result of the update operation, if any
     */
    updateShapeModel(shape: Shape, syncChanges?: boolean): any;
}
