import { Point } from './../math/Shapes';
export declare const LayoutDefaultOptions: any;
/**
 * Base class for layout algorithms.
 *
 * @type {*}
 */
export declare class LayoutBase {
    constructor();
    defaultOptions: any;
    options: any;
    currentHorizontalOffset: number;
    /**
     * Organizes the components in a grid.
     * Returns the final set of nodes (not the Graph).
     *
     * @param components
     */
    gridLayoutComponents(components: any[]): {
        nodes: any[];
        links: any[];
    };
    moveToOffset(component: any, p: any): Point;
    transferOptions(options: any): void;
}
