import { type Diagram } from "./../Diagram";
import { type Shape } from "./../dom/Shape";
import { FlowchartShapeType } from "./../dom/utils";
import { type Connection } from "./../dom/Connection";
import { type Rect, type Point } from "./../math/Shapes";
/**
 * The DiagramOptions interface defines the options for the Kendo Diagram component.
 */
export interface DiagramOptions {
    /**
     * Defines the default connection settings for the diagram.
     */
    connectionDefaults?: ConnectionDefaultsBase;
    /**
     * Defines the connections in the diagram.
     */
    connections?: Array<ConnectionOptionsBase>;
    /**
     * Defines editable settings for the diagram.
     */
    editable?: boolean | DiagramEditable;
    /**
     * Defines the diagram's layout settings.
     */
    layout?: DiagramLayout;
    /**
     * Defines the diagram's panning settings.
     */
    pannable?: boolean | Pannable;
    /**
     * Defines the diagram's selectable settings.
     */
    selectable?: boolean | Selectable;
    /**
     * Defines the diagram's shape defaults.
     */
    shapeDefaults?: ShapeDefaultsBase;
    /**
     * Defines the shapes in the diagram.
     */
    shapes?: Array<ShapeOptionsBase>;
    /**
     * Defines the tooltip settings for the diagram.
     */
    tooltip?: DiagramTooltip;
    /**
     * Defines the diagram's zoom.
     *
     * @default 1
     */
    zoom?: number;
    /**
     * Defines the maximum zoom level for the diagram.
     *
     * @default 2
     */
    zoomMax?: number;
    /**
     * Defines the minimum zoom level for the diagram.
     *
     * @default 0.1
     */
    zoomMin?: number;
    /**
     * Defines the zoom rate for the diagram.
     *
     * @default 0.1
     */
    zoomRate?: number;
}
/**
 * Defines the connectors available in the shape.
 * A connector is the point in the shape where a connection between this shape and another one can originate from or end.
 */
export interface ShapeConnector {
    /**
     * The connector name. The predefined names are:
     * - "top" - top connector
     * - "right" - right connector
     * - "bottom" - bottom connector
     * - "left" - left connector
     * - "auto" - auto connector
     */
    name?: string;
    /** The connector description. */
    description?: string;
    /** Function that positions the connector. Receives the shape as parameter and returns a point indicating where the connector should be placed. */
    position?: (shape: any) => any;
    /** The width of the connector.
     * @default 8
     */
    width?: number;
    /** The height of the connector.
     * @default 8
     */
    height?: number;
    /** The fill options of the connector. */
    fill?: string | Fill;
    /** The stroke options of the connector. */
    stroke?: string | Stroke;
    /** The hover configuration of the connector. */
    hover?: ShapeConnectorHover;
}
/**
 * Defines the padding options.
*/
export interface Padding {
    /** The top padding value. */
    top?: number;
    /** The right padding value. */
    right?: number;
    /** The bottom padding value. */
    bottom?: number;
    /** The left padding value. */
    left?: number;
}
/**
 * Defines the shapes content settings.
 */
export interface ShapeContent {
    /**
     * The alignment of the text inside the shape.
     * You can do combinations between "top", "middle" and "bottom" for vertical align and "right", "center" and "left" for horizontal align. For example, "top right", "middle left", "bottom center", and so on.
     */
    align?: string;
    /**
     * The color of the shape content text.
     */
    color?: string;
    /**
     * The font family of the shape content text.
     */
    fontFamily?: string;
    /**
     * The font size of the shape content text.
     */
    fontSize?: number;
    /**
     * The font style of the shape content text.
     */
    fontStyle?: string;
    /**
     * The font weight of the shape content text.
     */
    fontWeight?: string;
    /**
     * The template which renders the labels.
     */
    template?: string | ((data: any) => string);
    /**
     * The text displayed in the shape.
     */
    text?: string;
    /**
     * The spacing between lines of text in the shape.
     */
    lineSpacing?: number;
    /**
     * Configures the text wrapping behavior in the shape.
     *
     * @default "nowrap"
     */
    textWrap?: 'nowrap' | 'wrap';
    /**
     * The padding of the shape content.
     */
    padding?: number | Padding;
    /**
     * The relative padding of the shape content e.g. `0.03` means 3% of the shape width/height.
     */
    relativePadding?: number | Padding;
    /**
     * @hidden
     * The stroke options of the shape content.
     */
    stroke?: Stroke;
    /**
     * @hidden
     * The fill options of the shape content.
     */
    fill?: Fill;
    /**
     * @hidden
     * Specifies if the shape content should automatically resize to fit the text.
     */
    autoSize?: boolean;
}
/**
 * Defines the shape and connection editable options.
 * Allows configuring which editing actions are available in the diagram.
 */
export interface EditableDefaults {
    /**
     * Specifies if the shapes or connections can be dragged.
     * @default true
     */
    drag?: boolean;
    /**
     * Specifies if the shapes or connections can be removed.
     * @default true
     */
    remove?: boolean;
}
/**
 * Defines the shape editable options.
 * Allows configuring which editing actions are available for shapes in the diagram.
 */
export interface ShapeDefaultsEditable extends EditableDefaults, ShapeEditable {
}
/**
 * Defines the shape editable options.
 * Allows enabling or disabling the ability to create connections from the shape.
 */
export interface ShapeEditable {
    /**
     * Specifies if new connections can be added using the shape's connectors.
     * @default true
     */
    connect?: boolean;
}
/**
 * Defines a color stop for a shape gradient fill.
 * Used in the `stops` array of ShapeFillGradient.
 */
export interface ShapeFillGradientStop {
    /**
     * The stop offset from the start of the element. Ranges from `0` (start of gradient) to `1` (end of gradient).
     */
    offset?: number;
    /**
     * The color in any valid CSS format (name, hex, rgb, etc).
     */
    color?: string;
    /**
     * The fill opacity. Ranges from `0` (completely transparent) to `1` (opaque).
     */
    opacity?: number;
}
/**
 * Defines the gradient fill of the shape. Used in the `gradient` property of ShapeFill.
 */
export interface ShapeFillGradient {
    /**
     * The type of the gradient. Supported values are `"linear"` and `"radial"`. Default: `"linear"`.
     */
    type?: "linear" | "radial";
    /**
     * The center of the radial gradient. Coordinates are relative to the shape bounding box. For example, `[0, 0]` is top left and `[1, 1]` is bottom right.
     */
    center?: [number, number];
    /**
     * The radius of the radial gradient relative to the shape bounding box.
     *
     * @default 1
     */
    radius?: number;
    /**
     * The start point of the linear gradient. Coordinates are relative to the shape bounding box.
     */
    start?: [number, number];
    /**
     * The end point of the linear gradient. Coordinates are relative to the shape bounding box.
     */
    end?: [number, number];
    /**
     * The array of gradient color stops.
     */
    stops?: ShapeFillGradientStop[];
}
/**
 * Defines the fill options of the shape.
 * Use these settings to apply a single-color or a gradient background to shapes in the Diagram.
 */
export interface ShapeFill extends Fill {
    /**
     * Defines the gradient fill of the shape.
     */
    gradient?: ShapeFillGradient;
}
/**
 * Defines the hover configuration for the shape.
 * Allows you to specify the fill options when the shape is hovered.
 */
export interface ShapeHover {
    /**
     * Defines the hover fill options.
     */
    fill?: string | Fill;
}
/**
 * Defines the rotation of the shape.
 */
export interface ShapeRotation {
    /**
     * Sets the rotational angle of the shape.
     * @default 0
     */
    angle?: number;
}
/**
 * The fill options for shapes and connectors.
 */
export interface Fill {
    /**
     * Defines the fill color. Accepts valid CSS colors.
     */
    color?: string;
    /**
     * Defines the fill opacity. Ranges from `0` (completely transparent) to `1` (opaque).
     *
     * @default 1
     */
    opacity?: number;
}
/**
 * The stroke options for shapes and connectors.
 */
export interface Stroke {
    /**
     * Defines the color of the stroke. Accepts valid CSS colors.
     */
    color?: string;
    /**
     * The dash type of the stroke.
     */
    dashType?: DashType;
    /**
     * Defines the thickness or width of the stroke.
     * @default 1
     */
    width?: number;
    /**
     * Defines the line cap style of the stroke.
     */
    lineCap?: 'butt' | 'round' | 'square';
    /**
     * Defines the line join style of the stroke.
     */
    lineJoin?: 'bevel' | 'miter' | 'round';
}
/**
 * The dash types supported for strokes.
 */
export type DashType = "dash" | "dashDot" | "dot" | "longDash" | "longDashDot" | "longDashDotDot" | "solid";
/**
 * The built-in shape types.
 */
export type ShapeType = "rectangle" | "circle" | "image" | "text";
/**
 * Defines the hover configuration of the shape connectors.
 */
export interface ShapeConnectorHover {
    /**
     * Defines the hover fill options of the shape connectors.
     */
    fill?: string | Fill;
    /**
     * Defines the hover stroke options of the shape connectors.
     */
    stroke?: string | Stroke;
}
/**
 * Defines the default options for the shape connectors.
 */
export interface ShapeConnectorDefaults {
    /**
     * Defines the width of the shape connectors.
     * @default 8
     */
    width?: number;
    /**
     * Defines the height of the shape connectors.
     * @default 8
     */
    height?: number;
    /**
     * Defines the fill options of the shape connectors.
     */
    fill?: string | Fill;
    /**
     * Defines the stroke options of the shape connectors.
     */
    stroke?: string | Stroke;
    /**
     * Defines the hover configuration of the shape connectors.
     */
    hover?: ShapeConnectorHover;
}
/**
 * Defines the base default options that will be applied to all shapes in the Diagram.
 */
export interface ShapeDefaultsBase {
    /**
     * Defines the connectors the shape owns.
     * A connector is the point in the shape where a connection between this shape and another one can originate from or end.
     * You can define your own custom connectors or use the predefined types.
     */
    connectors?: ShapeConnector[];
    /**
     * Defines the default options for the shape connectors.
     */
    connectorDefaults?: ShapeConnectorDefaults;
    /**
     * Defines the default shapes content settings.
     */
    content?: ShapeContent;
    /**
     * Defines the shape editable options.
     */
    editable?: boolean | ShapeDefaultsEditable;
    /**
     * Defines the fill options of the shape.
     * Use these settings to apply a single-color or a gradient background to all shapes in the Diagram.
     */
    fill?: string | ShapeFill;
    /**
     * Defines the default height of shapes in the Diagram.
     * @default 100
     */
    height?: number;
    /**
     * Defines the hover configuration for the shape.
     */
    hover?: ShapeHover;
    /**
     * Defines the minimum height the shape can have.
     * Use this setting to apply a lower limit to the height of shapes when users resize them.
     * @default 20
     */
    minHeight?: number;
    /**
     * Defines the minimum width the shape can have.
     * Use this setting to apply a lower limit to the width of shapes when users resize them.
     * @default 20
     */
    minWidth?: number;
    /**
     * The path option of a Shape is a description of a custom geometry.
     * The format follows the standard SVG format.
     */
    path?: string;
    /**
     * Defines the rotation of the shape.
     */
    rotation?: ShapeRotation;
    /**
     * Specifies if Diagram shapes can be selected.
     * @default true
     */
    selectable?: boolean;
    /**
     * The path/URL to the shape image. Applicable when the type is set to "image".
     */
    source?: string;
    /**
     * Defines the configuration of the border around Diagram shapes.
     */
    stroke?: Stroke;
    /**
     * Specifies the type of the Shape using any of the built-in shape type.
     * @default "rectangle"
     */
    type?: ShapeType | FlowchartShapeType;
    /**
     * A function that returns a visual element to render for a given shape.
     */
    visual?: (data: any) => any;
    /**
     * Defines the default width of shapes in the Diagram.
     * @default 100
     */
    width?: number;
    /**
     * Defines the x-coordinate of shapes added to the Diagram.
     * @default 0
     */
    x?: number;
    /**
     * Defines the y-coordinate of shapes added to the Diagram.
     * @default 0
     */
    y?: number;
    /**
     * Defines the corner radius of the shape.
     * @default 0
     */
    cornerRadius?: number;
    /**
   * Defines the center position of the shape.
   * Applicable for the circle shape.
   */
    center?: Coordinate;
    /**
     * Defines the radius of the shape.
     * Applicable for the circle shape.
     */
    radius?: number;
}
/**
 * Defines the base shape options of the Diagram.
 */
export interface ShapeOptionsBase extends ShapeDefaultsBase {
    /**
     * The unique identifier for a Shape.
     * The id value is used to identify shapes in connection configurations.
     */
    id?: string;
    /**
     * Defines the shape editable options.
     */
    editable?: boolean | ShapeEditable;
    /**
     * Defines the shape dataItem.
     */
    dataItem?: any;
}
/**
 * Defines the Diagram selection options.
 *
 * By default, you can select shapes in the Diagram in one of two ways:
 * - Clicking a single shape to select it and deselect any previously selected shapes.
 * - Holding the `Ctrl` key while clicking multiple shapes to select them and any other shapes between them.
 *
 * Using the `selectable` configuration, you can enable single selection only, enable selection by drawing a rectangular area with the mouse around shapes in the canvas, or disable selection altogether.
 *
 */
export interface Selectable {
    /**
     * Defines the meta key when doing a visual selection through drawing a rectangular area around shapes in the canvas. This option does not change the way a single shape is selected when using click or tap. To avoid clashes, verify that the `selectable.key` and `pannable.key` are different.
     * The available values are:
     * - "none"  - No activation key, visual selection is disabled.
     * - "ctrl"  - The activation key will be Ctrl.
     * - "shift" - The activation key will be Shift.
     * - "alt"   - The activation key will be Alt.
     * @default "none"
     */
    key?: "none" | "ctrl" | "shift" | "alt";
    /**
     * Specifies if multiple selection is enabled.
     * @default true
     */
    multiple?: boolean;
    /**
     * Defines the selection line configuration.
     */
    stroke?: Stroke;
}
/**
 * Defines the pannable options. Use this setting to disable Diagram pan or change the key that activates the pan behavior.
 */
export interface Pannable {
    /**
     * If set to false, disables diagram panning.
     */
    enabled?: boolean;
    /**
     * Defines the meta key that will be used while panning the Diagram.
     * The available values are:
     * - "none"  - No activation key
     * - "ctrl"  - The activation key will be Ctrl
     * - "shift" - The activation key will be Shift
     * - "alt"   - The activation key will be Alt
     * @default "ctrl"
     */
    key?: "none" | "ctrl" | "shift" | "alt";
}
/**
 * Defines the grid layout options for diagram components.
 */
export interface LayoutGrid {
    /** The horizontal spacing between each component. */
    componentSpacingX?: number;
    /** The vertical spacing between each component. */
    componentSpacingY?: number;
    /** The left offset of the grid layout. */
    offsetX?: number;
    /** The top offset of the grid layout. */
    offsetY?: number;
    /** The width of the grid. */
    width?: number;
}
/**
 * The layout of a diagram consists in arranging the shapes (sometimes also the connections) in some fashion in order to achieve an aesthetically pleasing experience to the user.
 */
export interface DiagramLayout {
    /** The type of the layout algorithm to use. */
    type?: "tree" | "force" | "layered";
    /** The subtype further defines the layout type by specifying in greater detail the behaviour expected by the layout algorithm. */
    subtype?: "down" | "up" | "left" | "right" | "mindmapHorizontal" | "mindmapVertical" | "radial" | "tipOver" | "horizontal" | "vertical";
    /** The grid layout options. */
    grid?: LayoutGrid;
    /** The distance between siblings or levels. */
    horizontalSeparation?: number;
    /** The distance between siblings or levels. */
    verticalSeparation?: number;
    /** The height (in a vertical layout) or width (in a horizontal layout) between the layers. */
    layerSeparation?: number;
    /** The optimal length between 2 nodes (force-directed/layered layout). */
    nodeDistance?: number;
    /** The number of times that all the forces in the diagram are being calculated and balanced. */
    iterations?: number;
    /** Defines where the circle/arc starts (radial tree layout). */
    startRadialAngle?: number;
    /** Defines where the circle/arc ends (radial tree layout). */
    endRadialAngle?: number;
    /** Controls the distance between the root and the immediate children of the root (radial tree layout). */
    radialFirstLevelSeparation?: number;
    /** Defines the radial separation between the levels (radial tree layout). */
    radialSeparation?: number;
    /** The horizontal offset from a child with respect to its parent (tipOver tree layout). */
    underneathHorizontalOffset?: number;
    /** The vertical separation between siblings and sub-branches (tipOver tree layout). */
    underneathVerticalSeparation?: number;
    /** The vertical separation between a parent and its first child (tipOver tree layout). */
    underneathVerticalTopOffset?: number;
    /** Specifies the start level when the subtype is tipOver. */
    tipOverTreeStartLevel?: number;
}
/**
 * A set of settings to configure the Diagram behavior when the user attempts to edit, delete or create shapes and connections, drag shapes, resize shapes, or rotate shapes.
 */
export interface DiagramEditable {
    /** Specifies the connection editor template. */
    connectionTemplate?: (data: any) => string;
    /** Specifies if the shapes and connections can be dragged. */
    drag?: boolean | {
        snap?: boolean | {
            size?: number;
        };
    };
    /** Specifies if the shapes and connections can be removed. */
    remove?: boolean;
    /** Defines the look-and-feel of the shape resizing handles. */
    resize?: boolean | {
        handles?: {
            fill?: string | Fill;
            height?: number;
            width?: number;
            stroke?: Stroke;
            hover?: {
                fill?: string | Fill;
                stroke?: Stroke;
            };
        };
    };
    /** Specifies whether the shapes can be rotated. */
    rotate?: boolean | {
        fill?: Fill;
        stroke?: {
            color?: string;
            width?: number;
        };
    };
    /** Specifies the shape editor template. */
    shapeTemplate?: (data: any) => string;
}
/**
 * Defines the position of the connection label relative to the connection path.
 */
export interface ConnectionLabelPosition {
    /**
     * The vertical position of the label relative to horizontal connections.
     * - "top": Label appears above the connection (default)
     * - "bottom": Label appears below the connection
     * @default "top"
     */
    vertical?: 'top' | 'bottom';
    /**
     * The horizontal position of the label relative to vertical connections.
     * - "right": Label appears to the right of the connection (default)
     * - "left": Label appears to the left of the connection
     * @default "right"
     */
    horizontal?: 'left' | 'right';
}
/**
 * Defines the label displayed on the connection path.
 */
export interface ConnectionContent {
    /**
     * The color of the connection content text.
     */
    color?: string;
    /**
     * The font family of the connection content text.
     */
    fontFamily?: string;
    /**
     * The font size of the connection content text.
     */
    fontSize?: number;
    /**
     * The font style of the connection content text.
     */
    fontStyle?: string;
    /**
     * The font weight of the connection content text.
     */
    fontWeight?: string;
    /**
     * The template which renders the labels.
     */
    template?: (data: any) => string;
    /**
     * The static text displayed on the connection.
     */
    text?: string;
    /**
     * A function returning a visual element to render for the content of a connection.
     */
    visual?: (data: any) => any;
    /**
     * Defines the position of the label relative to the connection path.
     * @default { vertical: 'top', horizontal: 'right' }
     */
    position?: 'inline' | ConnectionLabelPosition;
    /** The background color of the connection label. Accepts valid CSS colors. */
    background?: string;
    /** The border options of the connection label.
     * @default null
    */
    border?: Stroke;
    /** The padding options of the connection label. Applicable when background or border is set.
     * @default { left: 4, right: 4, top: 2, bottom: 2 }
     */
    padding?: number | Padding;
    /**
     * Defines the distance (in pixels) between the label and the connection path.
     * @default 5
     */
    offset?: number;
}
/**
 * The connections end/start cap configuration.
 */
export interface ConnectionCap {
    /**
     * The end/start cap type used in connections.
     * The supported values are:
     * - "none": no cap
     * - "ArrowEnd" / "ArrowStart": a filled arrow
     * - "FilledCircle": a filled circle
     */
    type?: string;
    /**
     * The connections end/start cap fill options or color.
     */
    fill?: string | Fill;
    /**
     * The connections end/start cap stroke options or color.
     */
    stroke?: string | Stroke;
    /**
     * The SVG path data for the arrow marker.
     * Applies when the type is "ArrowEnd" or "ArrowStart".
     */
    path?: string;
    /**
     * The anchor point of the arrow marker.
     * This is the point where the marker will be positioned relative to the line.
     * Applies when the type is "ArrowEnd" or "ArrowStart".
     */
    anchor?: {
        x: number;
        y: number;
    };
    /**
     * The radius of the filled circle marker.
     * Applies when the type is "FilledCircle".
     * @default 4
     */
    radius?: number;
    /**
     * @hidden
     * The position of the marker on the path.
     * Can be "start" or "end".
     * @default "start"
     */
    position?: 'start' | 'end';
}
/**
 * Describes a point in the diagram using XY-coordinates.
 */
export interface Coordinate {
    /** The x-coordinate of the point. */
    x: number;
    /** The y-coordinate of the point. */
    y: number;
}
/**
 * Defines the base options for a connection in the diagram.
 */
export interface ConnectionOptionsBase extends ConnectionDefaultsBase {
    /** The source of the connection. Can be a shape id or an object with XY-coordinates. */
    from?: string | number | Coordinate;
    /** The target of the connection. Can be a shape id or an object with XY-coordinates. */
    to?: string | number | Coordinate;
    /** Sets the intermediate points (in global coordinates) of the connection. */
    points?: Array<Coordinate>;
    /** Defines the connection dataItem. */
    dataItem?: any;
}
/**
 * Defines the base defaults of the connections. Whenever a connection is created, the specified connectionDefaults will be used and merged with the (optional) configuration passed through the connection creation method.
 */
export interface ConnectionDefaultsBase {
    /** Defines the label displayed on the connection path. */
    content?: ConnectionContent;
    /** Defines the editing behavior of the connections. */
    editable?: boolean | EditableDefaults;
    /** The connections end cap configuration. */
    endCap?: string | ConnectionCap;
    /** Specifies the name of the source shape connector that should be used by default. */
    fromConnector?: string;
    /** Defines the default styling that is applied when the user hovers over a connection. */
    hover?: {
        stroke?: {
            color?: string;
        };
    };
    /** Specifies if connections can be selected. */
    selectable?: boolean;
    /** Defines the connections selection configuration. */
    selection?: {
        handles?: {
            fill?: string | {
                color?: string;
            };
            stroke?: {
                color?: string;
            };
            width?: number;
            height?: number;
        };
    };
    /** The connection start cap configuration or type name. */
    startCap?: string | ConnectionCap;
    /** Defines the connection line configuration. */
    stroke?: Stroke | string;
    /** Specifies the name of the target shape connector that should be used by default. */
    toConnector?: string;
    /**
     * Specifies the connections type. The supported values are:
     * - "polyline": connects the defined intermediate points.
     * - "cascading": discards given points and defines a cascading path between the endpoints.
     *
     * @default "cascading"
     * */
    type?: 'polyline' | 'cascading';
    /**
     * Defines the corner radius of the connection.
     *
     * @default 0
     */
    cornerRadius?: number;
}
/** Defines the tooltip settings for the diagram. */
export interface DiagramTooltip {
    /**
     * The delay in milliseconds before the tooltipShow event is triggered after the user hovers over a diagram item.
     * @default 200
     */
    delay?: number;
}
interface DiagramEvent {
    /**
     * @hidden
     * The diagram instance that triggered the event.
     */
    sender: Diagram;
    /** The point where the event occurred, in diagram coordinates. */
    point: Point;
    /** The native event that triggered the diagram event. */
    nativeEvent?: MouseEvent | TouchEvent | PointerEvent | WheelEvent;
    /** Additional metadata associated with the event. */
    meta?: any;
}
interface PreventableEvent {
    /**
     * Prevents the default action of the event.
     */
    preventDefault: () => void;
    /**
     * Checks if the default action of the event has been prevented.
     * @returns {boolean} True if the default action is prevented, false otherwise.
     */
    isDefaultPrevented: () => boolean;
}
/**
 * The event arguments for the `drag` event of the Diagram.
 */
export interface DiagramDragEvent extends DiagramEvent {
    /** The shapes being dragged. */
    shapes: Array<Shape>;
    /** The connections being dragged. */
    connections: Array<Connection>;
    /** The handle being dragged, if applicable. */
    connectionHandle?: string;
}
/**
 * The event arguments for the `dragStart` event of the Diagram.
 */
export interface DiagramDragStartEvent extends DiagramDragEvent, PreventableEvent {
}
/**
 * The event arguments for the `dragEnd` event of the Diagram.
 */
export interface DiagramDragEndEvent extends DiagramDragEvent, PreventableEvent {
}
/**
 * The event arguments for the DOM events of the Diagram.
 */
export interface DiagramDomEvent extends DiagramEvent {
    /** The item that was interacted with. */
    item?: Shape | Connection;
}
/**
 * The event arguments for the Tooltip events of the Diagram.
 */
export type DiagramTooltipEvent = DiagramDomEvent;
/**
 * The event arguments for the `zoomEnd` event of the Diagram.
 */
export interface DiagramZoomEndEvent extends DiagramEvent {
    /** The Diagram's zoom. */
    zoom: number;
}
/**
 * The event arguments for the `zoomStart` event of the Diagram.
 * This event is triggered before the zoom level changes.
 */
export interface DiagramZoomStartEvent extends DiagramZoomEndEvent, PreventableEvent {
}
/**
 * The event arguments for the pan event of the Diagram.
 */
export interface DiagramPanEvent extends Omit<DiagramEvent, "point"> {
    /** The Diagram's pan. */
    pan: Point;
}
/**
 * The event arguments for the select event of the Diagram.
 */
export interface DiagramSelectEvent extends Omit<DiagramEvent, "point"> {
    /** The selected shapes or connections in the Diagram. */
    selected: Array<Shape | Connection>;
    /** The deselected shapes or connections in the Diagram. */
    deselected: Array<Shape | Connection>;
}
/**
 * The event arguments for the itemBoundsChange event of the Diagram.
 */
export interface DiagramItemBoundsChangeEvent extends Omit<DiagramEvent, "point"> {
    /** The bounds of the item that changed. */
    bounds: Rect;
    /** The item that changed bounds. */
    item: Shape;
}
/**
 * The event arguments for the change event of the Diagram.
 */
export interface DiagramChangeEvent extends Omit<DiagramEvent, "point"> {
    /** The added shapes or connections to the Diagram. */
    added: Array<Shape | Connection>;
    /** The removed shapes or connections from the Diagram. */
    removed: Array<Shape | Connection>;
}
/**
 * Represents the options that can be used when bringing an item or area into view within the diagram.
 * Allows customization of alignment and animation behavior during the pan operation.
 */
export interface BringIntoViewOptions {
    /**
     * Controls the position of the calculated rectangle relative to the viewport.
     * Determines how the item(s) should be aligned when brought into view.
     * @default 'center middle'
     */
    align?: string;
    /**
     * Controls whether the pan operation should be animated.
     * @default false
     */
    animate?: boolean;
}
/**
 * Represents the serialized state of the diagram, including its shapes and connections.
 * Used for saving and restoring the diagram's structure.
 */
export interface DiagramState {
    /**
     * The array of shape definitions in the diagram.
     */
    shapes?: ShapeOptionsBase[];
    /**
     * The array of connection definitions in the diagram.
     */
    connections?: ConnectionOptionsBase[];
}
/**
 * Represents the options that can be used when selecting items in the diagram.
 */
export interface SelectionOptions {
    /**
     * If set to `true`, the item will be added to the current selection.
     */
    addToSelection?: boolean;
}
/**
 * Represents the possible directions for aligning diagram's elements.
 */
export type Direction = 'left' | 'right';
export {};
